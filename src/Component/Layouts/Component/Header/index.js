import { Container } from "react-bootstrap";

import NavBar from "./NavBar";

function Header() {
  return (
    <Container>
      <header>
        <NavBar />
      </header>
    </Container>
  );
}

export default Header;
